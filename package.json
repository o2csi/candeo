{
  "name": "candeo",
  "private": true,
  "version": "0.12.0",
  "description": "Lighting control, through direct HID access",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "pnpm --filter @candeo/desktop dev",
    "build": "pnpm --filter @candeo/desktop build",
    "tauri": "pnpm --filter @candeo/desktop tauri",
    "test:rust": "cargo test --workspace",
    "test:web": "pnpm --filter @candeo/desktop test",
    "test:packaging": "node --test \"packaging/**/*.test.mjs\"",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "site": "node site/build.mjs",
    "fmt:rust": "cargo fmt --all",
    "check": "pnpm lint:rust && pnpm test:rust"
  },
  "devDependencies": {
    "highlight.js": "^11.12.0",
    "marked": "^18.0.13"
  }
}